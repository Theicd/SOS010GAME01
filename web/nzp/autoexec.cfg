bind w +forward
bind s +back
bind a +moveleft
bind d +moveright
bind LEFTARROW +left
bind RIGHTARROW +right
bind UPARROW +lookup
bind DOWNARROW +lookdown
bind CTRL +attack
bind SPACE +jump
bindhold e +use
bind r +reload
bind MOUSE1 +attack
bind \ +mlook
+mlook
cl_yawspeed 90
cl_pitchspeed 90
sensitivity 3.2
